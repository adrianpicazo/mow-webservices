import { colors } from './Colors';

const colorFont = colors.BLUE_GREY.N500;

export const fonts = {

    HUGE_FONT: {
        fontSize: 24,
        color: colorFont
    },

    BIG_FONT: {
        fontSize: 18,
        color: colorFont
    },

    NORMAL_FONT: {
        fontSize: 12,
        color: colorFont
    }
};
